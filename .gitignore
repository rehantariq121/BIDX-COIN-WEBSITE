node_modules/
dist/
*.log
.DS_Store
output/

# Local env and tooling state
.env*
.vercel/

# Pre-optimisation masters. scripts/optimize-media.sh reads from here and writes
# the web-sized assets into public/; these originals are too large to ship.
media-source/

# Local agent/editor tooling state — not part of the project
.claude/
.codebase-memory/
.codex-runtime/

# TypeScript incremental caches and compiled config output
*.tsbuildinfo
vite.config.js
vite.config.d.ts
